function onWindowResize(){camera.aspect=window.innerWidth/window.innerHeight,camera.updateProjectionMatrix(),renderer.setSize(window.innerWidth,window.innerHeight)}var light_intesity=1,ambient_light_intensity=.1,al_red=.05,al_green=.05,al_blue=.01,scene=new THREE.Scene,camera=new THREE.PerspectiveCamera(75,window.innerWidth/window.innerHeight,.1,1e3),renderer=new THREE.WebGLRenderer;renderer.setSize(window.innerWidth,window.innerHeight);var stage=document.getElementById("stage");stage.appendChild(renderer.domElement);var geometry=new THREE.SphereGeometry(4.5,50,50),material=new THREE.MeshLambertMaterial({map:THREE.ImageUtils.loadTexture("img/textures/earth.jpg")}),cube=new THREE.Mesh(geometry,material);scene.add(cube),camera.position.z=10;var directionalLight=new THREE.DirectionalLight;directionalLight.color.setRGB(light_intesity,light_intesity,light_intesity),directionalLight.position.set(1,1,1).normalize(),scene.add(directionalLight);var ambientLight=new THREE.AmbientLight;ambientLight.color.setRGB(al_red,al_green,al_blue),scene.add(ambientLight),cube.rotation.x+=.5;var render=function(){requestAnimationFrame(render),cube.rotation.y+=6e-4,renderer.render(scene,camera)};render(),window.addEventListener("resize",onWindowResize,!1);